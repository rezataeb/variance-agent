# Copy to .env only if you use an env loader; never commit the real values.
PRISM_API_KEY=
PRISM_PROJECT_ID=
PRISM_HOST=https://prism.blockconvey.com

# Optional: GIDE/Ornith or another OpenAI-compatible model server.
OPENAI_BASE_URL=
OPENAI_API_KEY=EMPTY
OPENAI_MODEL=
